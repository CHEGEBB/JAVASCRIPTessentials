//Create an object that describes a train ticket and store it in the ticket variable. The object must have three fields:
 let trainTicket = {
    from :"nairobi",
    to :"Mombasa",
    price :600,
 };
 console.log(ticketTrian.from);
 console.log(trainTicket.to);
 console.log(trainTicket.price);