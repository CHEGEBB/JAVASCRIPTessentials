const pi = 3.142;// correct way to declare a constant a constant is simultaneously declared and initialized with a value that cannot be changed
console.log(pi);
// const gravity;
// gravity = 9.8;//this will throw an error because a constant must be declared and initialized at the same time
// console.log(gravity);
const gravity = 9.8;
// gravity=9.81;//this will throw an error because a constant cannot be changed
console.log(gravity);//correct way to declare a constant
